const ReconnectButton = ({ onClickTryToReconnectHandler }) => {
    return (
        <button onClick={onClickTryToReconnectHandler}>
            Reconnect
        </button>
    );
};

export default ReconnectButton;
