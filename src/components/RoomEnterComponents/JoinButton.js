const JoinButton = ({ onClickJoin }) => {
    return (
        <button style={{ marginTop: 20 }} onClick={onClickJoin}>
            Create or Join 
        </button>
    );
};

export default JoinButton;
